.venv/
__pycache__/
.DS_Store
node_modules/

# raw OSM extracts and derived layers (regenerable via scripts/01-02)
/data/

# large recomputable grids
out/dist_A.npy
out/dist_B.npy
out/land.gpkg

# wrangler local cache
.wrangler/

# Claude session config and local notes (public repo, never commit)
.claude/
CLAUDE.local.md

# pipeline working data: PBFs, layers, grids; regenerable from the snapshot identity
/work/
*.egg-info/

# dev harness output and the generated version stamp
/dev/out/
/site/version.json
